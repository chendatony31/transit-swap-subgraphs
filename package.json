{
  "name": "transit-swap",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ chendatony31/transit-swap",
    "create-local": "graph create --node http://localhost:8020/ chendatony31/transit-swap",
    "remove-local": "graph remove --node http://localhost:8020/ chendatony31/transit-swap",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 chendatony31/transit-swap",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.33.0",
    "@graphprotocol/graph-ts": "0.27.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
